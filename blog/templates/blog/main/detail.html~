{% extends "blog/main/main_base.html" %}
{% load thumbnail %}
{% load staticfiles %}
{% load oembed_tags %}


	{% block title %} {% if post %}{{ post.title }}{% endif %}{% endblock %}

	{% block content %}
		{% if post %}
			<div id="my-content" class="col pam content">
				
				<div class="my-post">
					<h1>{{ post.title }}</h1>
					{% if post.image %}
						<p class="my-cover"> 
							<img src="{{ post.image.url }}">
						</p>
					{% endif %}
					<p class="my-text">{% oembed 960x540 %} {{ post.text }} {% endoembed %}</p>
					<p class="my-tags">TAGS: {% for tag in post.tags.all %}<a href="{% url 'blog:tagged' tag.slug %}">{{ tag.name }} || </a>{% endfor %}</p>
					<p class="my-date">{{ post.date }}</p>
				</div>
				
			</div>
		{% endif %}
	{% endblock %}



