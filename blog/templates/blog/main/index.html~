{% extends "blog/main/main_base.html" %}
{% load thumbnail %}
{% load staticfiles %}
{% load oembed_tags %}

	{% block content %}	
		{% if latest %}
			<div id="my-content" class="col pam content">
				{% for post in latest %}
					<div class="my-post pa1 ma1">
						<h1><a href="{% url 'blog:detail' post.id %}">{{ post.title }}</a></h1>
						{% if post.image %}
						<p class="my-cover"> 
							<img src="{{ post.image.url }}">
						</p>
						{% endif %}
						<p class="my-text">{% oembed 960x540 %} {{ post.text }} {% endoembed %}</p>
						<p class="my-tags">TAGS:<br>{% for tag in post.tags.all %}<a href="{% url 'blog:tagged' tag.slug %}">{{ tag.name }}   </a>{% endfor %}</p>
						<p class="my-date">{{ post.date }}</p>
					</div>
				{% endfor %}
			
				{% if is_paginated %}
					<div class="my-pagination">
						{% if page_obj.has_previous %}
							<a href="{% url "blog:index" %}?page={{ page_obj.previous_page_number }}">previous</a>
						{% endif %}
							Page {{ page_obj.number }} of {{ page_obj.paginator.num_pages }}.
						{% if page_obj.has_next %}
							<a href="{% url "blog:index" %}?page={{ page_obj.next_page_number }}">next</a>
						{% endif %}
					</div>
				{% endif %}
			</div>
		{% endif %}
	{% endblock %}

	